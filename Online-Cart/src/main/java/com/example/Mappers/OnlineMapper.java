package com.example.Mappers;

public class OnlineMapper  {
	
	
	/*
	 * public String generateOrderNumber(String gender) { OrderSequnceDTO
	 * ticketSequence = orderSequnceDAO.findByid(gender); Long ticket =
	 * ticketSequence.getTicketIds(); Long ticketSeq = ++ticket; Date date = new
	 * Date(); String orderNo = gender + ticketSeq + String.valueOf(date.getYear() +
	 * 1900); ticketSequence.setTicketIds(ticketSeq);
	 * ticketSequnceDAO.save(ticketSequence); return ticketNo;
	 * 
	 * }
	 */

}
